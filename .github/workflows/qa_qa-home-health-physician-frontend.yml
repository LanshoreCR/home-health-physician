# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy Node.js app to Azure Web App - qa-home-health-physician-frontend

on:
  push:
    branches:
      - qa
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    environment: qa
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v5

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build
        run: bun run build
        env:
          VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL }}
          VITE_OKTA_ISSUER: ${{ vars.VITE_OKTA_ISSUER }}
          VITE_OKTA_CLIENT_ID: ${{ vars.VITE_OKTA_CLIENT_ID }}
          VITE_OKTA_REDIRECT_URI: ${{ vars.VITE_OKTA_REDIRECT_URI }}

      - name: Assemble deployment package
        run: |
          mkdir -p deploy
          cp -r dist deploy/dist
          cp server.js deploy/server.js
          cat > deploy/package.json <<'JSON'
          {
            "name": "home-health-physician",
            "private": true,
            "type": "module",
            "scripts": { "start": "node server.js" }
          }
          JSON

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v5
        with:
          name: node-app
          path: deploy

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment: qa

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v5
        with:
          name: node-app

      - name: 'Deploy to Azure Web App'
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'qa-home-health-physician-frontend'
          slot-name: 'Production'
          package: .
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_4536C7425C9E46D7A3D4EED21A8FBF02 }}
