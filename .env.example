VITE_API_BASE_URL=http://localhost:5024/api

# Okta (SPA public client, Authorization Code + PKCE)
VITE_OKTA_ISSUER=https://spchc.oktapreview.com
VITE_OKTA_CLIENT_ID=

# Opcional. Sin esto se deriva de window.location.origin + /login/callback, que es
# lo correcto en local y en cada ambiente desplegado. Definila solo si la app se
# sirve desde un origen distinto al registrado en Okta (slot de Azure, dominio propio).
# VITE_OKTA_REDIRECT_URI=

# Opcional. Sin esto se usa window.location.origin. Ese valor tiene que estar
# registrado en los Sign-out redirect URIs de la app en Okta.
# VITE_OKTA_POST_LOGOUT_REDIRECT_URI=
